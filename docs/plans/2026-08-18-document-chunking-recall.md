# 文档切分与召回草案（document-chunking-recall）

状态：Blocked
任务类型：C
（任务类型注记：C 探索实验——切分/召回链路与参数需实验验证，方向已由用户 2026-08-18 裁决）
最后更新：2026-09-21
关联 ADR：[ADR 0008](../history/adr/v0.1/0008-frontend-react-refactor.md)（九大界面：知识探索/知识点检索调试）、[ADR 0011](../history/adr/v0.1/0011-pending-design-location.md)（待评审设计留 plans/——本壳承载依据）
关联设计：[学习功能现状](2026-09-10-learning-center-current-state.md)（知识问答多 Agent 方向）、
[chunking-notes.md](../learning/chunking-notes.md)（切分策略）、
[rag-notes.md](../learning/rag-notes.md)（召回链路）、
[Axiom-Flow 交互全链路](2026-09-14-parsing-management-axiom-flow-chain.md)（解析产物与书目基座）
关联 Tracker：docs/trackers/todo.md（REQ-082 镜像行，第四轮主线 ARCH-021 承载）
归档判定：Retain（设计定稿评审通过后按 ADR 0011 以稳定名称迁 docs/design/，本壳含草案与
前置评估留 history/plans/ 追溯）

> 登记说明（2026-08-18 文档解析管理轮）：用户裁决「原始文档对照」删除、改为「探索」——
> 探索 = 文档切分 + 文档召回，通过对话方式进行，目的是整理知识；理论顺序为文档解析管理
> 部分走完后再做探索。本壳仅固设计方向与前置依赖，不动代码。

## 目标与成功标准

「探索」是管理中心的界面（管理中心菜单「探索」位），面向**单本已解析书目**：

- **文档切分**：把解析产物（blocks 结构化块）切成适合检索与学习的知识单元，检查切分
  边界是否截断完整句/公式（chunking-notes.md 的数学文档特殊性）；
- **文档召回**：通过**对话方式**对切分结果做检索验证——提问 → 召回相关块 → 展示命中
  与溯源（页/块级），人工判断召回是否准、是否全（rag-notes.md 关键点）；
- 目的：整理知识——切分与召回的产出（块级知识单元）是后续学习中心课程学习与知识问答
  的知识底座。

与[学习中心](2026-09-10-learning-center-current-state.md)知识问答的关系：探索是**管理侧的技术校验与知识整理**
（对单书逐块核对、调参、人工确认）；学习中心问答是**用户侧的最终形态**（多 Agent 编排、
课程级知识链路）。探索先行，问答后置。

成功标准（规划）：

- 切分：抽样 20 页无公式腰斩、无跨块语义断裂（对照 chunking-notes 验证方法）；
- 召回：冻结问题集（每书 10~20 题）命中率与 top-k 精度可观测、可对比参数；
- 溯源：答案/命中块可点击跳回原页原块（解析管理对照视图复用）。

## 范围与非目标

- **范围**：探索界面（`#/admin/explore`）设计与分阶段实施（v0 切分校验 → v1 简易召回 →
  v2 向量召回），块级知识单元产出登记。
- **非目标**：学习中心用户侧问答形态（归学习中心设计）；向量库容器编排（归 Axiom-Flow
  V2-009 方向）；本壳阶段不动代码。

### 界面形态（规划草案）

```
#/admin/explore（探索）
┌───────────────────────────────────────────────────────────────┐
│ 探索：{书目 Select}                     [切分参数] [新建对话]   │
├──────────────┬────────────────────────────────────────────────┤
│ 左：切分结果  │ 右：对话（召回验证）                            │
│ 块列表        │  Q: 什么是上确界？                             │
│  (段落/公式/  │  A: 来自 p0032 第 2 段：……（引用溯源可点击）    │
│   表格，按页  │     来自 p0033 公式 1.7：……                    │
│   折叠)       │  命中块高亮 + 相似度/来源标注                    │
│ 切分边界      │  追问/切换书目/调整切分参数再验证               │
│ 问题标记      │                                                │
└──────────────┴────────────────────────────────────────────────┘
```

- **切分**：以 blocks 为切分点（paragraph/formula/table 天然单元，Axiom-Flow 设计文档
  已明确「块级向量化，无需 LangChain 文本切分器」）；参数面板（chunk_size / overlap /
  分隔符策略）供实验，展示切分边界与截断警示（公式腰斩/跨块语义）。
- **召回**：对话方式——提问经检索链路（向量 + BM25 混合 → rerank）返回 top-k 块，
  展示命中块 + 引用溯源（书/页/块），人工判定召回质量；无向量库阶段可先用关键词/BM25
  简易检索做召回验证（chunking-notes 验证方法：抽样看切分边界 + 下游命中率）。
- **产出**：人工确认的知识单元（块 + 标签/备注）登记落库（af_* 扩展或学习表族，
  归属设计时裁决），供学习中心消费。

### 数据流与组件

```
8903 探索界面
 ├─ 书目列表：8900 → 8902 af_books（解析管理轮已建）
 ├─ 切分：8900 → 8902 块级产物（pages/{no}.blocks.json 经 /books/{id}/pages/{no}）
 ├─ 切分参数/预览：前端本地计算（块边界 + 公式完整性校验）或 8902 提供切分端点（待定）
 └─ 召回：检索链路（向量库 + BM25 + rerank）——归属 Axiom-Flow V2-009（Milvus）方向
     与根仓库学习中心检索链路，探索阶段以简易 BM25 先行（不依赖向量库容器）
```

## 前置条件

| 前置 | 状态 | 说明 |
| --- | --- | --- |
| 文档解析管理闭环（af_books 同步 + 块级对照 + 判定） | ARCH-020 收口中 | 书目基座与块级渲染复用 |
| 解析产物质量达标（公式可渲染率 ≥90%，V2-008） | Axiom-Flow 验收 | 召回质量依赖产物质量 |
| 检索链路选型（向量库 Milvus 等，V2-009 方向） | 探索项 | 阶段一可用 BM25/关键词简易召回 |
| LLM 网关（对话模型接入） | 已具备（注册表三接口） | 对话问答依赖，可用本地 Qwen 先行 |

## 工作项（各阶段独立立轮）

1. **探索 v0（切分校验）**：块级切分预览 + 边界/公式完整性检查 + 人工标记问题
   （依赖解析管理轮组件，纯前端 + 8902 只读）；
2. **探索 v1（简易召回）**：BM25/关键词对话式召回验证 + 溯源展示（不依赖向量库）；
3. **探索 v2（向量召回）**：向量库接入（Milvus，Axiom-Flow V2-009）+ 混合检索 + rerank，
   与学习中心检索链路合并评估（99% 目标）。

## 阻塞与恢复

阻塞证据：前置「文档解析管理闭环」未达用户验收（ARCH-020-E 待 REQ-075/080/081 回执），
第四轮主线 ARCH-021 未启动。
恢复条件：ARCH-021 启动评审通过（用户确认解析效果后立项探索 v0）。
责任位置：docs/trackers/todo.md（ARCH-021 / REQ-082）、Axiom-Flow 侧（REQ-075/080/081）。
复核触发点：ARCH-020-E 关闭时、第四轮主线启动轮立项时。

## 验证与验收

- 立轮实施时按阶段各自走 TDD 与浏览器验收（qed-frontend-check）；
- 本壳阶段验收 = 用户评审设计方向与分阶段口径。

## 回滚

纯设计草案，无实现副作用；如方向被否决，本壳按归档规则关闭并在 roadmap 标注取代关系。

## 关闭与归档

设计定稿（用户评审通过）后按 ADR 0011 晋升 `docs/design/`（稳定名称），本壳 Retain 归档
`history/plans/2026-08/`；阶段实施另立 PLAN 轮承接。
